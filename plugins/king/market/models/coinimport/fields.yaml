fields:

    update_existing:
        label: Update existing coins
        comment: Check this box to update posts that have exactly the same ID, or base.
        type: checkbox
        default: true
        span: left